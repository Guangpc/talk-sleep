# AI 好友睡前语音陪聊 MVP 产品需求文档（PRD）

> 状态：产品共识版 / MVP 设计基线
> 目标平台：中国大陆，iOS 优先；首版仅面向成年人（18+）
> 研究依据：[`research/ios-bedtime-voice-companion-constraints.md`](research/ios-bedtime-voice-companion-constraints.md)

## 1. 文档目的

本文将已确认的产品决策收敛为一份可供产品、设计、iOS、服务端、AI、隐私与测试团队执行的 MVP 需求。MVP 验证的是：用户是否愿意在睡前与一个由个人素材生成的 AI 好友自然地用语音聊天，并在会话结束后愿意再次使用；不验证医学意义上的睡眠检测。

本文中的"朋友"指用户创建的 AI 好友角色，不表示现实中的朋友正在通话，也不表示 AI 知道现实朋友的最新状态。

## 2. 一句话定位

一个让用户把熟悉的朋友语音与聊天风格转化为可编辑 AI 好友、在睡前通过手机自动监听进行中英双语语音聊天，并在用户停止说话后生成睡前记录与次日回顾的 iOS 应用。

## 3. 产品原则

1. **像朋友，但不冒充真人**：沉浸感来自音色、音调、语速、口头禅、幽默、观点与主动话题；产品界面持续显示"AI 好友"。
2. **自动语音优先**：会话只通过手机麦克风自动监听用户语音，不提供按住说话、点击说话或键盘输入作为 MVP 交互。文字只作为语音识别结果和 AI 回复的实时可视化。
3. **无固定倒计时，但有系统保护**：用户不看到单次或整场会话的固定时间上限；系统仍保留异常用量、服务稳定性、成本和安全熔断。
4. **先本地过滤，再最小化上传**：本地轻量 VAD、回声消除与音频切片；仅在检测到有效发言后上传语音片段。原始数据的云端临时处理、保存和训练用途必须分别披露。
5. **用户可控**：风格摘要、记忆、口头禅、主动话题和声音配置可查看、编辑、增加或删除；原始素材、AI 好友、聊天记录、总结和睡眠记录都可删除或导出。
6. **健康表达克制**：使用"可能入睡时间"和"一般睡眠建议"，不宣称医学睡眠检测，不作心理疾病或人格诊断。

## 4. 目标用户与核心场景

### 4.1 核心用户

- 中国大陆、18 岁以上、使用 iPhone 的用户；
- 有一位或多位让自己感到熟悉、放松的现实朋友；
- 愿意主动提供聊天截图、聊天文本和语音素材；
- 睡前偏好语音交流，不希望盯着屏幕打字；
- 可以接受 AI 好友并非真人、对素材外内容只能作不确定推测。

### 4.2 核心场景

用户打开 App，选择一个 AI 好友，选择"让 AI 先开场"或"我先说"。手机通过扬声器播放朋友风格的回复。用户说中文、英文或中英混合，AI 默认跟随用户语言；用户可以语音要求"用中文回答"或"用英文回答"。用户可以聊工作、学习、旅行、关系、观点和其他普通话题，也可以说"结束这个话题"。连续 9 分 30 秒无有效用户语音后，AI 轻声询问一次；再过 30 秒无回应，会话进入"可能已入睡"。第二天用户从锁屏通知点击"我醒了"，也可在 App 内确认、修改或补录起床时间。

## 5. MVP 范围

### 5.1 必须包含

- iPhone/iOS 原生体验，首版仅面向成年人；
- 首次启动的 AI 身份和数据处理说明；首次打开即申请麦克风权限，并在需要时申请语音识别（Speech）权限；
- 多个 AI 好友，一套素材对应一个 AI 好友；用户自定义名称并上传头像；持续显示"AI 好友"标识；
- 相册聊天图片；微信中用户主动选择后通过分享/导出进入 App；文字复制粘贴；音频文件导入；
- OCR、音频转写、说话人分析、自动区分用户和朋友；仅低置信度或明显冲突时请求用户确认；
- 声音配置、风格摘要、候选记忆、口头禅、观点/立场和主动话题生成；用户可查看、编辑、增加和删除；
- 允许原始朋友音频经加密临时云端处理，生成声音配置；用户决定是否保留原始素材；
- 自动监听用户语音；本地 VAD/AEC；只上传有效发言片段；
- 云端 ASR、对话生成、TTS/声音风格生成；
- 中文、英文和中英混合；AI 先开场或等待用户先说；AI 播放时可自然打断；
- 手机扬声器播放；首版目标支持锁屏后继续聊天；
- "正在聆听"、暂停聆听、结束聊天、停止播放；锁屏通知上的"我醒了"和结束控制；
- 9 分 30 秒无有效用户语音 + 30 秒轻声询问的睡眠状态机；
- 次日文字聊天记录、总结、可能入睡时间、起床时间、睡眠时长和一般睡眠建议；
- 下一晚推荐 2–3 个可跳过话题；
- 单次删除、全部删除、逐类删除、自动过期；Markdown/纯文本导出，数据层预留 JSON；
- 匿名产品指标；默认不训练，用户独立明确同意后才允许原始语音用于改进产品。

### 5.2 明确不做

- Android；
- AirPods/其他耳机专项体验，首版只承诺手机扬声器；
- 键盘主动文字输入、按住说话、点击说话模式；
- 多人群聊、Apple Watch；
- 医疗睡眠检测、睡眠分期、呼吸/心率监测；
- 心理疾病、人格或精神状态诊断；
- 自动通知家人、朋友、警方或其他第三方；
- 公开分享 AI 好友、声音、聊天记录或总结；
- 读取微信私有数据库；
- 默认保存聊天音频，但为未来可选保存预留接口；
- 订阅、收费、免费额度；
- 默认收集训练数据。

### 5.3 "实时文字输入"的定义

本 PRD 中"实时文字输入"特指用户使用键盘主动输入文字，因此属于 MVP 不做。用户说话后由 ASR 生成的实时转写文字不是键盘输入，而是 MVP 必须支持的"语音输入 + 实时文字转写"；AI 的文字回复也实时显示并由朋友风格语音朗读。

## 6. 核心对象与保存策略

| 对象 | 定义 | MVP 保存策略 |
|---|---|---|
| AI 好友 | 名称、用户头像、声音配置、风格、记忆、话题的角色 | 派生数据加密云存储；可逐类删除或删除整个 AI 好友 |
| 原始素材 | 朋友音频、聊天截图、复制文本、音频文件 | 处理结束后由用户选择保留或删除；保留时说明具体用途 |
| 有效用户语音 | 本地 VAD/AEC 判定为用户发言的片段 | 为实时 ASR 临时上传；不作为聊天音频历史默认保存 |
| 转写文本 | 导入素材或实时语音生成的文字 | 聊天历史默认仅文字本地保存，默认 30 天 |
| 风格摘要 | 语气、音调、语速、停顿、口头禅、幽默、话题和观点 | 加密云存储；可查看、编辑、增加、删除 |
| 记忆 | 从素材提取并经用户确认的共同经历或偏好 | 候选先确认；确认后可编辑、增加、删除 |
| 可能入睡时间 | 9:30 无有效语音加 30 秒无回应后的推测时间 | 显示"推测"，不等同实际入睡时间 |
| 睡眠记录 | 可能入睡、起床、时长、总结和建议 | 文字和总结默认本地保存 30 天；可改、删、导出 |

"健康数据""声音数据""截图/用户内容""转写和推断"要在实际实现中逐项完成 Apple App Privacy、隐私政策和数据流清单，不得因为它们被称为"配置"或"总结"就合并隐藏。[1]

## 7. 用户流程与功能

### 7.1 首次启动与权限

1. 展示产品价值、AI 好友身份说明和数据处理摘要。
2. 清楚说明：AI 好友基于用户提供素材生成，不是真人在通话；声音和风格只是模拟。
3. **首次打开 App 即申请麦克风权限**；语音识别权限在首次需要语音转文字时申请。拒绝时说明语音聊天不可用并提供系统设置入口，不切换到键盘或点击说话模式。
4. 展示"隐私与数据控制"入口：原始素材保留选择、逐类删除、全部删除、自动过期、导出和改进产品授权。

### 7.2 创建 AI 好友

1. 用户点击"创建 AI 好友"，输入自定义名称并上传头像；界面显示清晰的"AI 好友"标识。
2. 展示用户权利确认：商业版用户必须确认自己有权处理和使用所导入的声音、截图、文本及其中的第三方内容；提供举报、删除和滥用处理流程。
3. 选择素材来源：相册聊天截图；用户在微信中主动选择的内容通过系统分享/导出进入 App；复制粘贴文字；文件 App 中的音频文件。不读取微信私有数据库。
4. 显示导入队列、格式/大小限制、处理进度和取消按钮。
5. 自动处理链：OCR → 音频转写 → 说话人分析 → 自动区分用户/朋友 → 生成声音配置、风格摘要、候选记忆。
6. 自动分析完成后显示结果预览：素材覆盖度、角色区分置信度、声音质量、风格标签和候选记忆。
7. 只有低置信度或明显冲突时请求用户确认；用户可以确认谁是自己、谁是朋友，并重新标记关键片段。无须逐条人工标注。
8. 用户查看、编辑、增加、删除风格摘要、口头禅、主动话题、观点/立场、候选记忆和"不要主动提及"内容。
9. 生成 AI 好友；状态从"处理中"变为"可聊天"。
10. 处理完成后由用户选择保留或删除原始音频、截图和文字。选择保留时明确说明具体用途：重新生成声音配置、升级声音质量、更换模型、补充素材重新分析、纠正说话人识别、未来重新创建 AI 好友。

### 7.3 AI 好友列表

每张卡片显示：用户自定义名称、用户上传头像、清晰的"AI 好友"标签、处理状态、最近会话时间和数据管理入口。支持多个 AI 好友切换；一套素材只生成一个 AI 好友，禁止跨好友混用素材或记忆。

### 7.4 开始睡前会话

1. 选择 AI 好友，选择"让 AI 先开场"或"我先说"。
2. 进入"正在聆听"，显示名称、头像、AI 标识、麦克风/网络状态、暂停聆听和结束聊天。
3. 手机自动监听用户语音；不提供点击说话、按住说话或键盘输入入口。
4. 本地轻量 VAD/AEC 检测并切分用户发言；没有效语音的环境音直接丢弃。
5. 有效片段上传 ASR，用户转写文字实时显示，最终文字写入本地聊天记录。
6. AI 生成文字回复，在界面显示，并使用朋友的音色、音调、语速、停顿、口头禅和幽默风格朗读。
7. 默认跟随用户中文/英文/中英混合；用户可用语音指令指定 AI 回答语言。
8. 用户主动提出的普通话题都可处理；AI 不主动引导高风险内容。
9. 用户说"结束这个话题"时，AI 承认结束、不追问原因，并给出 2–3 个低压力选项；用户可直接指定新主题。
10. 素材外问题允许推测，但使用"我猜""可能""按以前的感觉"等明确不确定表达，不声称知道朋友现实中的最新情况。

### 7.5 自然打断与控制

- AI 朗读时检测到用户开始说话：立即降低音量并停止 TTS；
- 保留已经显示的文字；未播放部分标记为未完成朗读；
- 优先处理用户新发言，不要求重复；
- 用户可以暂停聆听或结束聊天；锁屏通知提供结束控制；
- 自动监听不是隐形监听：聊天页始终显示"正在聆听/已暂停/已结束"，并提供明显停止入口。

### 7.6 单次发言与会话时长

- 整场会话不显示固定倒计时或时长上限；
- 单次连续发言设置 3 分钟保护上限；接近上限时提示并自动分段，不结束总会话；
- 服务端保留不可见的故障、滥用、成本和稳定性熔断，触发时明确解释并保留已完成文字；
- 网络失败或 AI 超时保留当前会话，提示连接失败并提供重试；连续失败时允许结束或稍后继续，不生成假装听懂的回复。

### 7.7 睡眠状态机

```text
聊天中
  ├─ 用户有效语音 → 重置无声计时
  ├─ 暂停聆听 → 聆听暂停
  ├─ 结束聊天 → 主动结束
  └─ 连续 9:30 无有效用户语音
        ↓
轻声询问"你还醒着吗？"
  ├─ 30 秒内检测到有效用户语音 → 回到聊天中
  └─ 30 秒无回应
        ↓
可能已入睡（推测）
        ↓
等待确认起床
  ├─ 锁屏通知点击"我醒了" → 记录点击时间
  ├─ App 内点击"我醒了" → 记录点击时间
  ├─ 用户手动补录/修改 → 保存修订
  └─ 用户删除 → 删除本次记录
```

有效用户语音指用户对 AI 的有效发言；AI 播放回声、环境人声、翻身、咳嗽和清嗓不应重置计时。普通 VAD 不天然识别说话人，必须通过 AEC、回声抑制、VAD 和必要的说话人判定实测这一风险。

如果用户超过可能入睡时间 10 小时才打开 App，系统不把打开时间静默当作起床时间；用户仍可主动点击或手动补录。起床来源显示为锁屏点击、App 点击或手动补录。

### 7.8 次日总结、推荐和建议

进入"可能已入睡"后异步生成；网络不可用时延迟到下次联网或打开 App。总结包括：聊天主题、关键内容、谨慎的情绪/氛围观察、未完成话题、简短睡前回顾，以及下一晚 2–3 个可跳过的推荐话题。

只提供一般睡眠卫生、放松和作息建议，并显示"不是医学检测或医疗建议"。不做心理疾病、人格或医学判断，不推荐药物、补剂、治疗或处方调整。

### 7.9 起床与历史

- 用户可从锁屏通知或 App 内点击"我醒了"。
- 记录卡片显示：可能入睡时间（推测）、起床时间（用户点击/手动补录/未记录）、估算睡眠时长、聊天总结和建议。
- 起床时间可编辑；用户可以删除整条睡眠记录。
- 聊天文字记录和总结默认本地保存 30 天；提供 7/30/90 天或永久保留设置时，默认仍为 30 天。原始聊天音频不默认保存。
- 导出首版支持 Markdown 和纯文本；数据层预留结构化 JSON 导出接口。

## 8. 数据流与隐私要求

### 8.1 数据流

```text
截图/文本/朋友音频
  → 本地暂存和预处理
  → 经用户操作触发的加密临时云端 OCR/ASR/声音处理
  → 声音配置、风格摘要、候选记忆
  → 派生数据加密保存云端
  → 原始素材按用户选择保留

睡前麦克风
  → 本地 VAD/AEC/切片
  → 只上传有效用户语音片段
  → ASR 文字 → 对话模型 → TTS 音频
  → 文字实时显示、扬声器播放
  → 原始聊天音频默认不保存
  → 聊天文字/总结本地保存 30 天
```

### 8.2 保留和删除

- 原始朋友音频允许临时加密上传到自有处理服务生成声音配置；不得默认将其写入训练集、长期存储、备份或调试日志。
- 用户可选择保留原始素材；保留时明确说明重新生成声音、升级模型、补充素材、纠错和未来重建等具体用途。
- 用户可以逐类删除原始音频、截图、原始文本、声音配置、风格摘要、候选/确认记忆、转写文本、聊天记录、总结和睡眠记录，也可删除整个 AI 好友。
- 删除整个 AI 好友必须删除其派生配置、索引、embedding、缓存和关联请求；供应商异步删除时在界面显示状态。
- 聊天记录和总结默认本地保存 30 天；提供自动过期设置和单次/全部删除；首版不保存聊天音频。
- 首版支持 Markdown/纯文本导出；数据层预留结构化 JSON。

### 8.3 改进产品授权

默认不使用原始音频、截图和聊天内容训练或改进模型。提供独立、明确、可撤回的"帮助改进产品"开关；打开后说明收集哪些原始语音、用途、保留时间、接收方和删除方式。撤回后不再采集新的改进数据，已有数据走可见删除流程。

### 8.4 App Privacy 与权限清单

工程上线前按实际行为填报麦克风/Audio Data、照片或截图、用户内容/文本、账号标识符、诊断数据及睡眠推断相关数据。核对 `NSMicrophoneUsageDescription`、`NSSpeechRecognitionUsageDescription`、PhotosPicker、Speech 的本地/服务器处理、OCR/ASR/LLM/TTS/声音/存储/日志/分析供应商、`PrivacyInfo.xcprivacy`、App Store Connect App Privacy 和隐私政策的一致性。Speech 可能把语音发送至 Apple 服务器，因此不能笼统宣称完全本地。

## 9. iOS 技术和审核约束

### 9.1 推荐技术架构

推荐采用**本地轻量 VAD/AEC + 有效语音片段上传 + 云端 ASR/LLM/TTS**。本地不运行完整 LLM、声音克隆或高质量 TTS。20–30 ms 音频帧、单声道、短环形缓冲和轻量 VAD 通常比持续云端监听更节省网络与隐私成本；实际 CPU、内存、温升、电量和弱网表现必须在最低支持设备上实测。

AI 播放时继续监听用户打断，必须处理扬声器回声。VAD 只能判断是否有人声，不能天然证明声音来自用户；室友、电视和环境人声是必须单独验收的误报场景。

### 9.2 自动监听与扬声器

- 会话开始时配置输入/输出音频会话，启用回声消除、语音活动检测与中断处理；
- AI 播放期间继续监听用户打断，但避免把 AI 扬声器输出识别成用户输入；
- 录音状态在 App 内始终可见：`正在聆听`、`已暂停`、`已结束`；提供明显停止控制；
- iOS 系统麦克风指示不可隐藏；
- 不允许使用后台音频作为任意云推理或网络保活手段。

### 9.3 锁屏与后台

MVP 目标是手机锁屏后仍可继续语音聊天，并能从锁屏通知点击"我醒了"和结束聊天。Apple 文档支持适当配置音频会话与 `audio` background mode 的媒体播放场景，但后台录音、持续麦克风、网络推理和任意长时间执行并非自动获准，且后台服务必须用于规定目的。[1]

锁屏能力是 P0 体验目标，也是上线前技术/审核闸门：必须在真机验证前台/锁屏播放与采集、系统音频中断、低电量、网络变化和进程恢复；如实描述用户主动开始的语音会话、录音状态、停止控制和后台音频用途；不得隐藏麦克风状态，不得声称 iOS 能保证任何系统状态下永久监听；不得使用不透明 keep-alive 或不符合用途的后台模式；首版不提供点击说话模式作为后台失败替代。可接受的降级是明确提示会话被系统中断、保存已完成文字并允许用户重新开始。

### 9.4 声音权利和供应商

原始朋友音频可以临时加密上传自有处理服务生成声音配置；商业版要求用户确认自己有权使用声音，并提供举报、删除和滥用处理流程。平台也可以提供平台自有声音库。任何第三方声音供应商都必须核查授权、地域可用性、数据处理者/受托方角色、保留、训练、删除、跨境传输与 App Store 审核材料。Apple 没有为 AI 声音克隆提供通用安全港；AI 标识不替代第三方内容权利、误导和滥用评估。[1]

## 10. 安全、内容和责任边界

- AI 好友必须清晰标注为 AI，不声称自己就是现实朋友；
- 素材外信息必须用不确定表达；
- 用户主动提出的普通话题可聊，AI 不主动引导高风险内容；
- 自伤、危机或极端危险表达：温和回应，说明 AI 不能提供危机救援，建议联系当地急救、危机热线或可信任的人；没有明确授权、定位和人工响应机制时，不自动联系第三方；
- 医疗、心理诊断、睡眠疾病和用药问题不作诊断或治疗建议；
- 不公开分享 AI 好友、声音、聊天内容或总结；
- 中国大陆正式上线前，应由专业法律/隐私团队根据实际供应商、数据流、个人信息处理者身份、跨境路径和声音权利方案做专项评估。

## 11. 功能需求与验收标准

### 11.1 创建 AI 好友

| 编号 | 需求 | 验收标准 |
|---|---|---|
| F-001 | 多 AI 好友 | 至少创建 2 个好友；A 的素材、记忆和声音不会出现在 B 的会话上下文 |
| F-002 | 多来源导入 | 可导入相册截图、微信主动分享/导出内容、粘贴文本、音频文件；私有数据库不在权限和实现范围 |
| F-003 | 自动素材分析 | 处理链可产出 OCR/转写、角色区分、风格摘要、候选记忆和声音配置 |
| F-004 | 低置信度确认 | 低置信度或冲突时显示待确认项；确认后才写入角色配置 |
| F-005 | 可编辑角色 | 用户可编辑/增加/删除口头禅、风格、话题、立场描述、记忆和禁提内容 |
| F-006 | 原始素材选择 | 处理结束明确询问保留/删除；保留时展示用途，删除可逐类执行 |

### 11.2 语音会话

| 编号 | 需求 | 验收标准 |
|---|---|---|
| F-101 | 全自动监听 | 会话中不显示点击说话或按住说话入口；用户自然说话即可生成输入 |
| F-102 | VAD 过滤 | 环境静音/普通噪声不发送；AI 回声不被当作用户输入；用户连续讲话能切片 |
| F-103 | 实时转写 | 用户发言出现实时转写，最终文字可回看；键盘输入入口不存在 |
| F-104 | 中英切换 | 中文、英文、中英混合可识别；语音指令可指定 AI 回答语言 |
| F-105 | 自然打断 | TTS 中检测用户语音后立即降音并停止，保留显示文字，优先处理新发言 |
| F-106 | 话题结束 | 说"结束这个话题"后不追问，提供 2–3 个新选项；用户可直接指定新话题 |
| F-107 | 控制状态 | 始终可见"正在聆听"；可暂停聆听、结束聊天；播放可停止；锁屏有结束操作 |
| F-108 | 无固定时长 | UI 不显示倒计时/单次固定上限；服务端仍有熔断，且熔断时提供可理解提示 |
| F-109 | 单段保护 | 连续发言达到 3 分钟前提示并自动分段，不结束总会话、不要求重新开始 |
| F-110 | 错误恢复 | 网络或 AI 超时保留会话状态，提供重试；连续失败后可结束或稍后继续，不生成假装听懂的回复 |

### 11.3 睡眠记录

| 编号 | 需求 | 验收标准 |
|---|---|---|
| F-201 | 无声判定 | 9:30 无有效用户语音后播放一次轻声询问；再 0:30 无回应进入可能已入睡 |
| F-202 | 可恢复 | 询问后检测到用户语音，回到聊天，不错误结束 |
| F-203 | 睡眠标注 | 可能入睡时间显示为推测，不显示医学检测结论 |
| F-204 | 锁屏醒来 | 锁屏通知可点击"我醒了"；App 内同样可点击；点击时间可修改/删除 |
| F-205 | 10 小时规则 | 超过 10 小时打开 App 不静默填写起床时间；用户仍可主动补录，来源明确 |
| F-206 | 总结 | 可能入睡后生成主题、关键内容、氛围观察、未完成话题、回顾和 2–3 个下一晚话题 |
| F-207 | 建议 | 只输出一般睡眠卫生/放松建议，明确非医学检测/医疗建议 |

### 11.4 数据控制

| 编号 | 需求 | 验收标准 |
|---|---|---|
| F-301 | 保留期限 | 聊天文字和总结默认本地 30 天；原始聊天音频不默认保存 |
| F-302 | 删除 | 可单次、全部、逐类删除聊天、总结、原始素材、声音配置、风格和记忆、AI 好友 |
| F-303 | 导出 | 首版导出 Markdown/纯文本；数据接口预留 JSON |
| F-304 | 改进授权 | 默认不训练；独立开关可授权原始语音改进，授权可撤回 |
| F-305 | 匿名指标 | 默认只收集会话完成、响应延迟、崩溃、权限失败、后台中断等不含聊天内容的指标 |

## 12. 非功能需求

### 12.1 性能与可靠性（TestFlight 观测指标）

- 已授权用户首次成功开始会话率 ≥90%；
- 首段 AI 音频响应：中位数 ≤2.5 秒，P95 ≤5 秒；
- 用户语音最终转写错误导致的主动纠正率 ≤20%（按中英分层采样）；
- 正常弱网重试后会话恢复率 ≥95%；
- 会话崩溃率 ≤1%，后台中断事件可观测且不丢失已确认文字；
- 首次创建 AI 好友成功率 ≥80%（素材达到最低质量要求的样本）；
- 7 日内再次开启睡前会话比例作为主要留存观察指标，首轮目标 ≥30%；
- "像朋友""聊天自然""帮助放松"三项主观评分均以 5 分制收集，目标中位数 ≥4；
- 删除与导出操作成功率 100%，失败必须可重试并有明确状态；
- 在最低支持设备上完成持续监听、锁屏、扬声器播放和中断恢复的功耗/温升专项验收，具体设备阈值在技术 Spike 后冻结。

### 12.2 安全与可审计

- 传输 TLS，云端派生数据静态加密；密钥与业务数据分离；
- 不把原始音频、截图、转写全文写入普通日志、崩溃日志或分析事件；
- 每个数据类别记录收集点、目的、接收方、是否离设备、是否关联、追踪状态、保留期限、删除传播和供应商是否可训练；
- 供应商访问最小化并可审计；
- 支持隐私政策、用户删除请求和数据导出；
- App 审核账号/演示素材使用虚构信息，不使用真实朋友聊天截图、头像或声音作为商店元数据。

## 13. 事件与指标设计

默认只发送匿名、聚合或随机安装标识关联的产品事件，不发送文本内容、音频、截图、声音 embedding、记忆或总结。建议事件：`onboarding_permission_result`、`friend_import_started/completed/failed`、`friend_confirmation_requested/completed`、`session_started/ended/interrupted`、`voice_turn_transcribed`（只含延迟、语言和成功状态）、`assistant_first_audio_latency`、`sleep_inference_entered`、`wake_confirmed/edited/deleted`、`summary_generated/failed`、`raw_material_retention_choice`（只含选择类型）、`data_exported/deletion_requested/deletion_completed`、`improvement_consent_granted/revoked`。

## 14. 风险、闸门与开放实施项

### P0 风险

1. **锁屏持续麦克风与后台网络链路**：必须通过真机和审核材料验证；后台播放可行不等于后台持续录音和推理自动获准。
2. **真人声音克隆权利与滥用**：用户声明、供应商条款、投诉和删除流程必须在商业 TestFlight 前完成；用户上传任意声音是高风险能力，不应只依赖"AI 标识"。
3. **"用户语音"与环境语音区分**：轻量 VAD 识别是否有人声，不天然识别说话人；必须测试扬声器回声、室友、电视和夜间噪声。不能为实现 10 分钟睡眠判定而隐蔽收集环境音。
4. **云端数据最小化实际落地**：确认自有服务、ASR、LLM、TTS、声音供应商不会将请求写入训练集、长期日志、备份或跨租户缓存。
5. **健康数据边界**：睡眠推断、总结和指标不能误导为医疗功能；若将睡眠数据视为健康信息，不能放入不合适的 iCloud/广告/分析路径。[1]

### P1 实施项

- 最低支持 iPhone 与 iOS 版本；
- 语音流式协议、断线重连和幂等；
- 导入格式/大小与 OCR/ASR 质量门槛；
- 声音配置版本、重新生成和回滚；
- 云端派生数据的逐类删除传播；
- 本地加密数据库和 30 天过期任务；
- 锁屏通知 action 与会话状态恢复；
- Markdown/纯文本导出内容格式；
- 简体中文优先的危机与权限文案；
- `PrivacyInfo.xcprivacy`、App Privacy 和供应商数据地图。

## 15. 测试计划

### 15.1 关键真机矩阵

- 新款与最低支持 iPhone；
- 前台/后台/锁屏；
- 扬声器低、中、高音量；
- 安静房间、风扇、电视、室友说话、咳嗽、翻身；
- 中文、英文、中英混合、口音、轻声说话；
- 用户打断 AI 的开头/中段/结尾；
- Wi-Fi、蜂窝网络、弱网、断网、恢复网络；
- 来电、闹钟、系统音频抢占、低电量；
- 麦克风/Speech/照片权限允许、拒绝、撤回；
- 删除、导出、自动过期和重装后的状态。

### 15.2 隐私验证

- 抓包确认静音帧和环境音未上传；
- 供应商侧确认临时请求删除、无训练、备份/日志策略；
- 验证删除 AI 好友后声音配置、embedding、索引、缓存和备份删除状态；
- 验证改进产品开关撤回后不再上传训练数据；
- 核对 App Privacy、隐私政策、权限文案、隐私清单和实际行为；
- 使用审核演示数据，不将真实朋友素材放入 App Store 截图或审核包。

## 16. 版本规划

### MVP（当前）

个人实验到封闭 TestFlight；中国大陆；iOS；18+；多 AI 好友；素材导入；自定义声音临时云处理；自动语音会话；扬声器与锁屏目标；睡眠记录；文字总结；数据控制；匿名指标；不收费。

### 后续版本候选

- 耳机/AirPods 体验；
- 更丰富的声音库与授权角色；
- 结构化 JSON 导出；
- 可选聊天音频保存；
- 订阅：免费用户限时、订阅用户无固定体验上限；
- 更完善的设备端 ASR/VAD；
- 在充分审核和权利设计后扩展分享能力。

## 17. 研究依据

[1] [`research/ios-bedtime-voice-companion-constraints.md`](research/ios-bedtime-voice-companion-constraints.md)，其中汇总并链接 Apple App Review Guidelines §§2.5.1、2.5.4、2.5.14、5.1.1–5.1.3、5.2，AVFAudio/Speech/PhotosPicker/后台音频官方文档，Apple App Privacy 与 Privacy Manifest 文档，以及 GDPR、CCPA/CPRA 一手资料。

> 合规提示：本文是产品需求和工程约束，不是法律意见。中国大陆正式上线前应由具备相关资质的法律/隐私团队根据实际供应商、数据流、个人信息处理者身份、跨境路径和声音权利方案完成专项评估。
